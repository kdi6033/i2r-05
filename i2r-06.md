# i2r-06 Arduino 프로그램 사양서

이 문서만으로 board-i2r-06-hmi.ino 를 재현할 수 있도록 작성된 완전한 기술 사양서입니다.

---

## 1. 보드 기본 정보

| 항목 | 값 |
|------|-----|
| 모델명 | i2r-06 |
| 버전 | 1.6 |
| MCU | ESP32-S3 |
| 파일명 | board-i2r-06-hmi.ino |

---

## 2. 하드웨어 구성

### 2-1. I2C 핀 (SHT30 + OLED 공용)
| 신호 | GPIO |
|------|------|
| SDA  | 18   |
| SCL  | 17   |

### 2-2. SHT30 온습도 센서
- I2C 주소: `0x44`
- 측정 명령: `0x2C 0x06` (Single Shot, Clock Stretching Disabled, High Repeatability)
- 응답: 6바이트 (tempMSB, tempLSB, CRC, humiMSB, humiLSB, CRC)
- 온도 변환: `T = -45 + 175 * (raw / 65535.0)`
- 습도 변환: `H = 100 * (raw / 65535.0)`
- 소수점 1자리 반올림 후 저장 (`round(x * 10) / 10.0`)
- 측정 대기: `delay(50)` (측정 명령 후)

### 2-3. OLED 디스플레이
- 드라이버: SH1106, 1.3인치, 128x64, I2C
- 라이브러리: U8g2 (`U8G2_SH1106_128X64_NONAME_F_HW_I2C`)
- 생성자: `U8G2_R0, U8X8_PIN_NONE, SCL=17, SDA=18`

### 2-4. 기타 핀
| 용도 | GPIO |
|------|------|
| LED  | 2    |
| 공장 초기화 버튼 (TRIGGER_PIN) | 41 |

### 2-5. 아이콘 비트맵
- **Bluetooth 아이콘** (9x13 px, PROGMEM):
```cpp
const unsigned char bt_icon[] = {
  0x08,0x00, 0x18,0x00, 0x28,0x00, 0x48,0x00,
  0x88,0x00, 0x50,0x00, 0x20,0x00, 0x50,0x00,
  0x88,0x00, 0x48,0x00, 0x28,0x00, 0x18,0x00, 0x08,0x00
};
```
- **WiFi 아이콘** (16x13 px, PROGMEM, 이름: `wifi_rounder`):
```cpp
const unsigned char wifi_rounder[] = {
  0x00,0x00, 0xF0,0x0F, 0x0C,0x30, 0x02,0x40,
  0x00,0x00, 0xE0,0x07, 0x18,0x18, 0x00,0x00,
  0xC0,0x03, 0x20,0x04, 0x00,0x00, 0x80,0x01, 0x80,0x01
};
```

---

## 3. DeviceProfile (i2r-06 고정값)

```
profile.hasSensor    = true
profile.hasDisplay   = true   (OLED u8g2)
profile.inputCount   = 0
profile.outputCount  = 0
profile.triggerPin   = 41
sensorNames[0]       = "temp"
sensorNames[1]       = "humi"
numberOfSensors      = 2
maxPins              = 8
numberOfPins         = 0      (outputCount와 동기화)
```

- 입출력 핀 없음 (현재 릴레이 미장착)
- 다른 모델(i2r-02, i2r-03, i2r-04)의 설정 코드도 setupDeviceConfig() 안에 유지해야 함 (공통 코드베이스)

---

## 4. 사용 라이브러리

```cpp
#include "SPIFFS.h"
#include "certificate.h"       // root_ca, client_cert, private_key 문자열 상수
#include "esp_system.h"        // esp_get_free_heap_size()
#include <ArduinoJson.h>
#include <FS.h>
#include <HTTPClient.h>
#include <HTTPUpdate.h>
#include <Preferences.h>
#include <PubSubClient.h>
#include <WiFi.h>
#include <WiFiClientSecure.h>
#include <Wire.h>
#include <algorithm>
#include <U8g2lib.h>
#include <math.h>
#include <NTPClient.h>
#include <WiFiUdp.h>
#include <map>
#include <time.h>
#include <vector>
#include <BLEDevice.h>
#include <BLEServer.h>
#include <BLEUtils.h>
#include <Adafruit_AHTX0.h>    // i2r-03용, i2r-06에서는 SHT30 직접 구현
```

---

## 5. 전역 변수

```cpp
float temperature = 0.0;   // SHT30 원시 온도 (Sensor::measure 내 임시)
float humidity    = 0.0;   // SHT30 원시 습도 (Sensor::measure 내 임시)

Preferences preferences;
String ca_pem, cert_pem, key_pem;
long utcOffsetInSeconds = 3600 * 9;   // KST (NVS에서 덮어씀)
WiFiUDP ntpUDP;
NTPClient timeClient(ntpUDP, "pool.ntp.org", utcOffsetInSeconds);
const unsigned long ntpUpdateInterval = 3600000;  // 1시간

WiFiClientSecure espClient;
PubSubClient client(espClient);

// 핀 배열
int outputPins[8] = {26,27,32,33,21,22,23,25};
int inputPins[8];   // setupDeviceConfig()에서 초기화
bool pinStates[8]         = {false,...};
bool previousPinStates[8] = {false,...};
```

---

## 6. 클래스 / 구조체 구성

### 6-1. Sensor
```
name        : String ("temp" / "humi")
raw         : float  (보정 전 측정값)
current     : float  (raw + cal, 표시/송신에 사용)
cal         : float  (보정값, SPIFFS /sensor-cal.json에 저장)
previousValue : float (-1로 초기화)

measure()   : SHT30에서 raw 측정 → current = raw + cal
loop()      : measure() 호출
```

### 6-2. SensorManager
```
sensors         : vector<Sensor>
sensorHighLows  : vector<SensorHighLow>
nextSlotIndex   : int
lastTimeSensor  : unsigned long
intervalSensor  : 5000ms (5초)
lastTimeTick    : unsigned long
intervalTick    : 30000ms (30초)
aht             : Adafruit_AHTX0 (i2r-03 전용, i2r-06에서 미사용)
lastSentValues  : map<String, float>
ENV_DEADBAND    : 0.5f (히스테리시스)

setup()         : i2r-06 → Wire.begin(18,17), u8g2.begin(), 센서 등록, 보정값/트리거 조건 로드
loop()          : 5초 주기 측정, 30초 주기 tickEnvStatus()
addSensor()
getSensorByName()
getSensorValue()
calibrateSensor() : s->cal = valueCali - s->raw; saveCalibrations()
saveCalibrations() : SPIFFS /sensor-cal.json
loadCalibrations() : SPIFFS /sensor-cal.json
saveAllSensorConfigs() : SPIFFS /sensor-config.json
loadAllSensorConfigs() : SPIFFS /sensor-config.json
insert(JsonObject)
list(typeSensor)
deleteOne(slotIndex)
deleteAllByType(typeSensor)
tickEnvStatus() : previousValue와 current 차이 >= 0.5 이면 sendStatusCheckChange(false)
```

### 6-3. SensorHighLow
```
slotIndex     : int
name          : String ("temp" / "humi")
current       : float
trigger       : bool (true=상승, false=하강)
triggerValue  : float
durationSec   : int
delay         : int (초)
isScheduled   : bool
scheduledTime : unsigned long
scheduledRise : bool
last          : float (-1)
portHigh      : vector<PinStateChange>
portLow       : vector<PinStateChange>

loop(SensorManager&) : 상승/하강 감지 → delay>0 이면 예약, 아니면 sendOutChange()
sendOutChange(rise)  : 같은 MAC → digitalWriteUpdateData(), 다른 MAC → MQTT "so"
```

### 6-4. WifiMqtt
```
isConnected          : bool
isConnectedMqtt      : bool
isDownloading        : bool
ssid, password, email, mqttBroker : String
outTopic[50], inTopic[50] : char[] ("i2r/<email>/out", "i2r/<email>/in")
lastBlinkTime        : unsigned long
statusSendCounter    : unsigned long
startupTime          : unsigned long
ignoreDuration       : 5000ms
bootVersionReported  : bool
lastMqttRetryTime    : unsigned long
mqttRetryInterval    : 60000ms (1분)
lastWifiRetryTime    : unsigned long
wifiRetryInterval    : 60000ms (1분)
wifiFailCount        : int  (10회 연속 재시도 실패 → ESP.restart())
mqttFailCount        : int  (10회 연속 MQTT 실패 → ESP.restart())

connectToWiFi()  : WiFi.begin() → 최대 3회 × 500ms 폴링 → 연결 실패 시 false로 루프 진입
loop()           : WiFi 미연결 → 1분 간격으로 재시도 후 실패 카운트; 연결 시 MQTT 유지
reconnectMQTT()  : client.connect(mac) → 성공 시 subscribe(inTopic), 버전 1회 전송
publishMqtt()    : sendData에 email/type/mac 추가 → mac 비교 후 outTopic or inTopic 발행
readWifiMacAddress() : WiFi.mode(WIFI_STA) → dev.mac 설정
```

**WiFi WATCHDOG 규칙:**
- `wifiFailCount++`는 `wifiRetryInterval`(1분) 마다 실제 재시도 후에만 증가
- 10회 누적 실패 시 `ESP.restart()`
- WiFi 연결 성공 시 카운터 0으로 리셋

### 6-5. Ble
```
service_uuid         : "4fafc201-1fb5-459e-8fcc-c5c9c331914b"
characteristic_uuid  : "beb5483e-36e1-4688-b7f5-ea07361b26a8"
boot                 : bool (BLE 페어링 모드 활성 여부)
isConnected          : bool

setup()              : ble.boot=true, BLEDevice 초기화, 광고 시작
readBleMacAddress()  : Serial 출력
writeToBle(order)    : order=1 → WiFi 정보, order=2 → sendData, 0/101/102 → 문자열
```

### 6-6. Device
```
mac          : String (WiFi MAC, 대문자)
lastTime     : unsigned long
interval     : 100ms
out[8], in[8] : int
sendData, sendDataPre : String
pendingOutputs : vector<PendingOutput>
scheduledAutoOffs : vector<ScheduledAutoOff>
pinStateChanges[maxPins+numberOfSensors][2] : vector<PinStateChange>
nextPendingSlotIndex : int

loop()                     : 100ms마다 입력 스캔(inputCount=0이므로 실질 루프만), sendStatusCheckChange(true)
sendStatusCheckChange(bool) : 변경 감지 후 MQTT 발행
digitalWriteUpdateData(pin, value, durationSec) : 출력 설정, duration>0이면 scheduledAutoOff 등록
processScheduledAutoOffs() : 예약 시각 도달 시 자동 OFF
processPendingExecutions() : exec=true인 PendingOutput 처리
checkFactoryDefault()      : TRIGGER_PIN LOW 감지 → 버튼 시간에 따라 초기화
loadPinStatesFromSPIFFS()  : /pinState_N.json 로드
sendOut(port, portState)   : pinStateChanges 기반 출력 실행
```

### 6-7. DisplayManager
```
lastSentTime     : unsigned long
displayInterval  : 5000ms

setup()     : i2r-06 → u8g2.begin(), u8g2.enableUTF8Print()
loop()      : 5초마다 updateOled() 호출 (i2r-06 전용 분기)
updateOled(): OLED 화면 그리기 (아래 레이아웃 참조)
sendState() : i2r-03/04 전용, Serial1 HMI로 JSON 전송 (i2r-06에서 미사용)
```

### 6-8. TimeSlot / TimeSlotManager
```
TimeSlot: startHour/Min/Sec, endHour/Min/Sec, repeatMode("daily"/"weekly"), dayOfWeek
timeSlots[maxPins] : vector<TimeSlot> 배열

TimeSlot::setup()  : NVS에서 tz_offset/savedTime 로드, NTP forceUpdate, SPIFFS 타임슬롯 로드
TimeSlot::loop()   : 1초마다 스케줄 활성 여부 확인 → digitalWriteUpdateData()
                     WiFi 연결 시 1시간마다 NTP 갱신

TimeSlotManager::insert/deleteOne/deleteAll : SPIFFS /timeslots_N.json 저장
```

### 6-9. Config
```
loadConfigFromNVS()  : NVS "netcfg" → wifi.ssid/password/email/mqttBroker
saveConfigToNVS()    : NVS "netcfg" 저장 → ESP.restart()
initializeSPIFFS()   : SPIFFS.begin() 실패 시 format 후 재시도
savePendingOutputsToFile(portNo) : /bio_N.json
loadPendingOutputsFromFile(portNo) : /bio_N.json
```

### 6-10. BioManager
```
insert(JsonObject) : PendingOutput + pinStateChanges 등록, /bio_N.json 저장
list(portNo)       : pendingOutputs 순회, 각각 MQTT 발행
deleteOne(portNo, slotIndex) : removePendingOutput + 파일 저장
deleteAll(portNo)  : 해당 포트 전체 삭제, pinStateChanges 초기화, 파일 저장
```

### 6-11. Tool
```
download_program(fileName) : HTTPS OTA 업데이트
  URL: "https://github.com/kdi6033/download/raw/main/" + fileName
  onStart/onEnd/onProgress/onError 콜백 등록
  isDownloading 플래그로 MQTT 발행 차단
  Progress 30초 정지 → 타임아웃 처리
blinkLed(iteration) : LED 100ms on/off 반복
```

---

## 7. OLED 화면 레이아웃 (128x64)

```
┌─────────────────────────────────┐ y=0
│                                 │
│  온도 :  25.3 °C                │ y=22  font: unifont_t_korean2
│                                 │
│  습도 :  60.2 %                 │ y=44  font: unifont_t_korean2
│                                 │
│  HH:MM   [BT]  mqtt  [WiFi]     │ y=51/63
└─────────────────────────────────┘ y=63
```

하단 아이콘 좌표:
| 항목 | x | y | 크기 | 조건 |
|------|---|---|------|------|
| 시간 (HH:MM) | 5 | 63 | font 6x10_tf | lastNtpTime > 1000000000 |
| BT 아이콘   | 63 | 51 | 9x13 | ble.boot || ble.isConnected |
| "mqtt" 텍스트 | 76 | 63 | font 8x13_tf | wifi.isConnectedMqtt |
| WiFi 아이콘 | 110 | 51 | 16x13 | wifi.isConnected |

BLE 모드(ble.boot=true)에서는 온도/습도 `--.-` 표시

---

## 8. SPIFFS 파일 구조

| 파일 | 내용 |
|------|------|
| `/sensor-cal.json` | `{"cal":{"temp":0.0,"humi":0.0}}` |
| `/sensor-config.json` | SensorHighLow 트리거 조건 목록 |
| `/bio_N.json` | 포트 N의 PendingOutput 목록 (`{"ps":[...]}`) |
| `/pinState_N.json` | 포트 N의 PinStateChange 목록 (`{"changes":[...]}`) |
| `/timeslots_N.json` | 포트 N의 TimeSlot 목록 (JSON 배열) |
| `/factory_reset.txt` | 공장 초기화 후 BLE 모드 진입 플래그 ("1") |

---

## 9. NVS (Preferences) 키

| 네임스페이스 | 키 | 타입 | 내용 |
|-------------|-----|------|------|
| "netcfg" | "ssid"       | String | WiFi SSID |
| "netcfg" | "password"   | String | WiFi 패스워드 |
| "netcfg" | "email"      | String | MQTT 이메일 |
| "netcfg" | "mqttBroker" | String | MQTT 브로커 주소 |
| "certs"  | "root_ca"    | String | TLS Root CA PEM |
| "certs"  | "client_cert"| String | TLS 클라이언트 인증서 PEM |
| "certs"  | "private_key"| String | TLS 개인 키 PEM |
| "i2r"    | "tz_offset"  | Long   | UTC 오프셋 초 (기본 32400=KST) |
| "i2r"    | "savedTime"  | ULong  | NTP 실패 시 복원용 epoch |

---

## 10. MQTT 토픽

| 방향 | 토픽 |
|------|------|
| 발행 (outTopic) | `i2r/<email>/out` |
| 구독 (inTopic)  | `i2r/<email>/in`  |

- 포트: 8883 (TLS)
- 클라이언트 ID: dev.mac
- 부팅 후 1회 버전 전송: `{"c":"gs","m":"<mac>","e":"<email>","ver":"1.6"}`

---

## 11. MQTT 명령 프로토콜 (parseJSONPayload)

모든 명령은 JSON 형식, `"c"` 필드로 구분. MAC 주소가 있으면 자기 MAC과 비교 후 불일치 시 무시.

### si — setInfo (WiFi 설정 저장)
```json
{"c":"si","ssid":"...","password":"...","e":"email","mqttBroker":"..."}
```
→ NVS 저장 후 재부팅

### df — downloadFirmware (OTA)
```json
{"c":"df","m":"<mac>","f":"firmware.bin"}
```
→ `https://github.com/kdi6033/download/raw/main/<f>` 에서 OTA 업데이트

### so — setOutput (출력 제어)
```json
{"c":"so","m":"<mac>","n":0,"v":1,"d":5}
```
- `n`: 출력 포트 인덱스
- `v`: 1=ON, 0=OFF
- `d`: 유지 시간(초), 생략 시 0 (무제한)

### bio — bindIO (입력→출력 연동)
```json
{"c":"bio","o":"insert","m":"<mac>","n":0,"tr":1,"d":3,"du":4,"ps":[{"m":"<mac>","n":0,"v":1}]}
{"c":"bio","o":"list","m":"<mac>","n":0}
{"c":"bio","o":"delete","m":"<mac>","n":0,"sI":1}
{"c":"bio","o":"deleteAll","m":"<mac>","n":0}
```
- `n`: 입력 포트
- `tr`: 1=HIGH 트리거, 0=LOW 트리거
- `d`: 지연 시간(초)
- `du`: 출력 유지 시간(초)
- `ps`: 출력 포트 목록

### bs — bindSensor (센서→출력 연동)
```json
{"c":"bs","o":"insert","ts":"temp","tr":1,"tv":30.0,"du":4,"d":3,"m":"<mac>","ps":[{"m":"<mac>","n":0,"v":1}]}
{"c":"bs","o":"list","ts":"temp","m":"<mac>"}
{"c":"bs","o":"delete","ts":"temp","sI":1,"m":"<mac>"}
{"c":"bs","o":"deleteAll","ts":"temp","m":"<mac>"}
{"c":"bs","o":"cali","ts":"temp","v":25.5,"m":"<mac>"}
```
- `ts`: 센서 타입 ("temp"/"humi", "temperature"/"humidity"도 허용)
- `tr`: 1=상승(≥triggerValue), 0=하강(≤triggerValue)
- `tv`: 트리거 임계값
- `du`: 출력 유지 시간(초)
- `d`: 지연 시간(초)
- `cali.v`: 실제 기준값 → `cal = v - raw`로 보정

### sch — schedule (시간 스케줄)
```json
{"c":"sch","o":"insert","m":"<mac>","pi":0,"start":36000,"end":72000,"rm":"daily"}
{"c":"sch","o":"insert","m":"<mac>","pi":0,"start":36000,"end":72000,"rm":"weekly","dw":[1,3]}
{"c":"sch","o":"list","m":"<mac>","pi":0}
{"c":"sch","o":"delete","m":"<mac>","pi":0,"sI":1}
{"c":"sch","o":"deleteAll","m":"<mac>","pi":0}
```
- `pi`: 출력 포트 인덱스
- `start`/`end`: 초 단위 (예: 36000=10:00:00)
- `rm`: "daily" 또는 "weekly"
- `dw`: 요일 배열 (0=일, 1=월, ..., 6=토), weekly일 때만

### gs — getStatus (상태 요청)
```json
{"c":"gs","m":"<mac>"}
```
응답:
```json
{"c":"gs","m":"<mac>","ver":"1.6","time":"HH:MM:SS","tz":32400}
```

### st / setTime — 시간 동기화
```json
{"c":"st","m":"<mac>","epoch":1700000000,"tz_offset":32400}
```
→ NVS 저장, 내부 시각 즉시 갱신

### ti — touchInput (내부 명령, OLED 화면/BLE에서만)
```json
{"c":"ti","req":"config"}        // WiFi 설정 정보 요청
{"c":"ti","bleboot":1}           // BLE 페어링 모드 전환 요청
```

---

## 12. 상태 전송 JSON 형식

MQTT outTopic으로 발행되는 기기 상태:
```json
{
  "m": "<WiFi MAC>",
  "temp": 25.3,
  "humi": 60.2,
  "e": "<email>",
  "t": "i2r-06",
  "fr": "<mac>"
}
```
- i2r-06은 inputCount=0, outputCount=0 이므로 "in"/"out" 배열 없음
- 센서값 변화 없으면 발행 안 함 (deadband 0.5 적용)

---

## 13. setup() 순서

```
1. setupDeviceConfig()
2. Serial.begin(115200)
3. pinMode(TRIGGER_PIN, INPUT), pinMode(ledPin, OUTPUT)
4. SPIFFS.begin() → 실패 시 format 후 재시도
5. factory_reset.txt 존재 확인 → isFactoryReset 플래그
6. config.loadConfigFromNVS()
7. display.setup()  [hasDisplay=true]
8. ssid 비어있는 경우:
   - isFactoryReset=true → ble.setup() → display.updateOled() → return (BLE 대기)
   - isFactoryReset=false → 오프라인 모드, wifi.readWifiMacAddress()
9. ssid 있는 경우: wifi.connectToWiFi()
10. outputPins pinMode OUTPUT (outputCount=0 이므로 skip)
11. inputPins  pinMode INPUT  (inputCount=0 이므로 skip)
12. config.loadPendingOutputsFromFile() (inputCount=0 이므로 skip)
13. sensorManager.setup()
14. NVS "certs" 로드 → espClient 인증서 설정
15. client.setServer(mqttBroker, 8883), client.setCallback(callback)
16. wifi.startupTime = millis()
17. wifi.reconnectMQTT()
18. timeSlot.setup()
19. dev.loadPinStatesFromSPIFFS()
```

---

## 14. loop() 순서

```
1. OTA 다운로드 중 → return
2. 힙 < 20KB → ESP.restart()
3. 7일(millis > 604800000) → ESP.restart()
4. ble.boot=true → display.loop() + delay(50) + return
5. dev.checkFactoryDefault()
6. dev.loop()
7. wifi.loop()
8. sensorManager.loop()  [hasSensor=true]
9. display.loop()         [hasDisplay=true]
10. timeSlot.loop()
```

---

## 15. 공장 초기화 버튼 (TRIGGER_PIN=41)

| 누름 시간 | 동작 |
|-----------|------|
| 100ms 이상 ~ 3초 미만 | WiFi 설정만 초기화 (NVS "netcfg" clear) + BLE 모드 재부팅 |
| 3초 이상 | SPIFFS format + NVS 전체 초기화 + BLE 모드 재부팅 |

재부팅 전 현재 epoch 시간을 NVS "i2r"/savedTime 에 저장 (시간 유지)

---

## 16. BLE 서비스 정보

```
Device Name  : "i2r-06-AI Sensor"
Service UUID : 4fafc201-1fb5-459e-8fcc-c5c9c331914b
Char UUID    : beb5483e-36e1-4688-b7f5-ea07361b26a8
Properties   : READ | WRITE
```

BLE 수신 데이터는 parseJSONPayload()로 직접 처리

---

## 17. OTA 다운로드 URL

```
https://github.com/kdi6033/download/raw/main/<filename>
```

- TLS: `clientSecure.setInsecure()` (인증서 검증 무시)
- Progress 30초 미진행 시 타임아웃 처리
- 결과는 MQTT `{"c":"df_result","ok":1}` 또는 `{"ok":0}` 로 발행

---

## 18. 시간 관리

- NTP 서버: `pool.ntp.org`
- 기본 타임존: UTC+9 (KST = 32400초)
- 부팅 시 `timeClient.forceUpdate()` 1회 시도
- WiFi 연결 상태에서 1시간마다 갱신
- NTP 실패 시: NVS savedTime 복원 (재부팅 2초 보정)
- 오프라인 시: `lastNtpTime + (millis() - lastMillis) / 1000`으로 소프트 클럭 유지

---

## 19. LED 동작

| 상태 | LED |
|------|-----|
| MQTT 연결됨 | 1초 간격 점멸 |
| MQTT 미연결 | OFF |
| 부팅 시작 | HIGH (켜짐) |

MQTT 연결 직후 `sendStatusCheckChange(false)` 3회 전송 (statusSendCounter < 3)

---

## 20. certificate.h 구조

```cpp
// certificate.h
const char root_ca[] PROGMEM = R"EOF(
-----BEGIN CERTIFICATE-----
...
-----END CERTIFICATE-----
)EOF";

const char client_cert[] PROGMEM = R"EOF(
-----BEGIN CERTIFICATE-----
...
-----END CERTIFICATE-----
)EOF";

const char private_key[] PROGMEM = R"EOF(
-----BEGIN RSA PRIVATE KEY-----
...
-----END RSA PRIVATE KEY-----
)EOF";
```

NVS에 저장된 인증서와 현재 펌웨어 인증서가 다르면 자동 업데이트

---

## 21. 주요 타이밍 요약

| 항목 | 주기 |
|------|------|
| 센서 측정 | 5초 |
| 히스테리시스 체크 | 30초 |
| OLED 갱신 | 5초 |
| Device 루프 | 100ms |
| WiFi 재시도 | 1분 |
| MQTT 재시도 | 1분 |
| NTP 갱신 | 1시간 |
| 자동 재시작 | 7일 |
| WiFi 연속 실패 재시작 | 10회 (각 1분 간격) |
| MQTT 연속 실패 재시작 | 10회 |
| 힙 부족 재시작 | 20KB 미만 |
| 부팅 초기 메시지 무시 | 5초 |
